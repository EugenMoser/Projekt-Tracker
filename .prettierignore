node_modules/
dist/
build/
coverage/
.expo/
pnpm-lock.yaml

# Generated by drizzle-kit — never hand-edited, never formatted.
# The SQL migrations are checksummed in the journal, so reformatting them
# would invalidate the migration history.
**/drizzle/
**/migrations/*.sql

# Claude-Arbeitsdateien (lokal, nicht im Remote — siehe .git/info/exclude)
.claude/
CLAUDE.md
